version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - gradle build
  post_build:
    commands:
      - echo Build completed successfully   
artifacts:
  files:
    - target/book-management-service.jar
  discard-paths: yes